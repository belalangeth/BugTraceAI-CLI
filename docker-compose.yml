services:
  api:
    build: .
    container_name: bugtrace_api
    init: true          # run tini so orphaned chromium procs get reaped (no zombie pile-up)
    shm_size: 2gb       # chromium renderers use /dev/shm; the 64MB default exhausts -> renderer wedge -> hang
    ports:
      - "8000:8000"
    volumes:
      - bugtraceai-cli_cli-scans:/app/scans
      - bugtraceai-cli_cli-reports:/app/reports
      - bugtraceai-cli_cli-db:/app/data
      - bugtraceai-cli_cli-logs:/app/logs   # persist scanner logs (execution.log / bugtrace.jsonl / errors.log) across rebuilds — enables saturation/timeout forensics
      - /var/run/docker.sock:/var/run/docker.sock
      - ./.env:/app/.env   # bind the host .env so Provider-tab key changes (update_env_var writes /app/.env) persist across restarts
      - ~/.codex:/root/.codex:ro   # Codex CLI login (codex auth login) for the codex provider
    env_file:
      - .env
    environment:
      BUGTRACE_CORS_ORIGINS: ${BUGTRACE_CORS_ORIGINS:-http://localhost:5173,http://localhost:6869}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

  mcp:
    build: .
    container_name: bugtrace_mcp
    command: ["mcp", "--sse", "--host", "0.0.0.0", "--port", "8001"]
    ports:
      - "8001:8001"
    volumes:
      - bugtraceai-cli_cli-reports:/app/reports
      - bugtraceai-cli_cli-logs:/app/logs
      - bugtraceai-cli_cli-db:/app/data
    env_file:
      - .env
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sS -m 2 http://localhost:8001/sse >/dev/null; res=$$?; [ $$res -eq 0 ] || [ $$res -eq 28 ] || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped

volumes:
  bugtraceai-cli_cli-scans:
  bugtraceai-cli_cli-reports:
  bugtraceai-cli_cli-logs:
  bugtraceai-cli_cli-db:
